# Sprint 4 Group W — System Administration, Compliance and Alumni Pipeline
# Test Cases

**BRD:** MEETCS-BRD-CRM-001 v1.0
**Group:** W | **Module:** System Administration (SA) + Compliance (CR) + Alumni Pipeline (AL)
**Last Updated:** 2026-04-23

---

## Test Case Table

| ID | Req ID | Scenario | Preconditions | Steps | Expected Result | Status |
|---|---|---|---|---|---|---|
| W-001 | SA-001 | Institution admin views institution profile | User logged in with `institution-admin` role | Navigate to **Admin → Institution** | Profile page loads showing name, logo, timezone, locale, and branding fields | ⬜ Planned |
| W-002 | SA-001 | Institution admin edits and saves institution profile | User logged in with `institution-admin` role; profile page open | 1. Change institution name. 2. Click **Save**. | Success flash shown; updated name persists on page reload; audit log records the change with old and new values | ⬜ Planned |
| W-003 | SA-001 | Counsellor cannot edit institution profile | User logged in with `counsellor` role | Navigate to **Admin → Institution** and attempt to submit the edit form | HTTP 403 Forbidden; no change saved | ⬜ Planned |
| W-004 | SA-001 | Cross-tenant data isolation | Two institutions (A and B) seeded; user from institution A logged in | Query `Lead` model from authenticated context | Returns zero rows belonging to institution B; `InstitutionScope` enforced | ⬜ Planned |
| W-005 | SA-002 | Add a new campus | `institution-admin` logged in | Navigate to **Admin → Campuses → Add Campus**; fill in name, code, city; click **Save** | New campus appears in the campus list with status Active | ⬜ Planned |
| W-006 | SA-002 | Edit an existing campus | Active campus exists | Open campus edit form; update city field; click **Save** | Updated city reflected in campus list; audit entry created | ⬜ Planned |
| W-007 | SA-002 | Deactivate a campus | Active campus exists | Click **Deactivate** on campus record | Campus status changes to Inactive; campus no longer selectable in lead/programme forms | ⬜ Planned |
| W-008 | SA-003 | Create a new academic year | `institution-admin` logged in | Navigate to **Admin → Academic Years → Create**; enter label, start date, end date; click **Save** | Academic year record created with status Closed; visible in list | ⬜ Planned |
| W-009 | SA-003 | Set an academic year as active | One academic year exists with status Closed | Click **Set Active** on the year | Selected year status changes to Active; any previously active year is set to Closed | ⬜ Planned |
| W-010 | SA-003 | Roll over to a new academic year | An active academic year with seat counts exists | Run `crm:rollover-academic-year {institution} {new_year_label}` Artisan command | New academic year created; seat counts copied from source year; pipeline stage counters reset to zero; `rolled_over_from_id` populated | ⬜ Planned |
| W-011 | SA-004 | View audit trail for a lead | `institution-admin` logged in; at least one lead field edited | Navigate to **Admin → Audit Trail**; filter by entity type "Lead" | Table shows rows with user, action, model ID, changed fields (old → new values), IP address, and timestamp | ⬜ Planned |
| W-012 | SA-004 | Audit trail is read-only | `institution-admin` logged in | Attempt to edit or delete any audit log entry via UI or direct HTTP request | No edit/delete controls present; direct HTTP requests return HTTP 403 | ⬜ Planned |
| W-013 | SA-004 | Filter audit trail by date range | Audit logs exist spanning multiple dates | Apply a date range filter on the audit trail page | Only entries within the selected date range are displayed | ⬜ Planned |
| W-014 | SA-005 | Import leads from a valid CSV | `institution-admin` logged in; valid CSV with correct headers prepared | Navigate to **Admin → Data Import**; upload CSV; click **Import** | All rows imported successfully; success summary shows count; no error report generated | ⬜ Planned |
| W-015 | SA-005 | Import CSV with invalid headers | CSV file with incorrect column headers prepared | Upload CSV via the import form | Import rejected before row processing; error message lists missing or unrecognised headers | ⬜ Planned |
| W-016 | SA-005 | Import CSV with mixed valid and invalid rows | CSV with 10 rows, 3 containing missing required fields | Upload CSV and trigger import | Valid 7 rows imported; error report generated listing the 3 failed rows with row numbers and reason | ⬜ Planned |
| W-017 | SA-005 | Export leads to CSV | Leads exist in the system | Navigate to **Admin → Data Export**; select entity "Leads"; choose CSV format; click **Export** | CSV file downloaded containing all lead records scoped to the current institution | ⬜ Planned |
| W-018 | SA-006 | Update a system configuration value | `institution-admin` logged in | Navigate to **Admin → System Configuration**; change timezone setting; click **Save** | Success flash displayed; new timezone value persists after page reload; `system_configs` record updated | ⬜ Planned |
| W-019 | SA-006 | Non-admin cannot access system configuration | User logged in with `counsellor` role | Navigate to **Admin → System Configuration** | HTTP 403 Forbidden; page not accessible | ⬜ Planned |
| W-020 | SA-008 | Create a custom field for leads | `institution-admin` logged in; no existing custom fields | Navigate to **Admin → Custom Fields → Add Field**; set entity type "Lead", label "Preferred Intake", type "Select"; click **Save** | Custom field appears on the lead creation form for the institution; other institutions are unaffected | ⬜ Planned |
| W-021 | SA-008 | Reorder custom fields via drag-and-drop | Two or more custom fields exist for same entity type | Drag second field above first in the order list; save order | `sort_order` values updated in database; fields render in new order on the lead form | ⬜ Planned |
| W-022 | SA-009 | Create a notification template with merge tags | `institution-admin` logged in | Navigate to **Admin → Notification Templates → Create**; select channel "Email"; enter subject with `{{lead.name}}`; enter body text; click **Save** | Template saved; `merge_tags_json` column populated with detected tags | ⬜ Planned |
| W-023 | SA-009 | Preview a notification template | Notification template with merge tags exists | Open template; click **Preview** | Preview renders with placeholder values substituted for merge tags (e.g. `{{lead.name}}` → "John Doe") | ⬜ Planned |
| W-024 | SA-012 | Manually trigger a backup | `super-admin` logged in | Navigate to **Admin → Backups**; click **Run Now** | `DatabaseBackupJob` dispatched; backup log entry created; completed backup listed with download link after job finishes | ⬜ Planned |
| W-025 | CR-001 | Consent captured on lead creation | Lead creation form open; consent checkbox visible | Fill in lead details; tick the consent checkbox; submit form | `ConsentRecord` created with `consented_at`, `ip_address`, `form_version`, and `consent_type = DataProcessing`; lead saved | ⬜ Planned |
| W-026 | CR-002 | Consent record stores IP and form version | Lead created with consent in step W-025 | Navigate to lead profile → Consent History | Consent record shows correct IP address, user agent, form version, and ISO timestamp | ⬜ Planned |
| W-027 | CR-003 | Opt-out request creates log and is processed within SLA | Authenticated lead in portal | Lead submits opt-out request via portal | `OptOutLog` record created with `requested_at`; `ProcessOptOutJob` dispatched; `processed_at` populated within 24 hours; lead communication preferences updated | ⬜ Planned |
| W-028 | CR-003 | Manual opt-out processing by admin | Pending opt-out requests exist | Admin navigates to **Compliance → Opt-Out Requests**; clicks **Process Now** | Selected opt-out processed immediately; `processed_at` set; lead preferences updated | ⬜ Planned |
| W-029 | CR-004 | Data access request submitted and compiled data shown | `institution-admin` logged in; lead record exists | Navigate to **Compliance → Data Access Requests → New**; select lead; submit | `DataAccessRequest` record created; `DataAccessService` compiles lead data package; compiled data viewable by admin; delivery method logged | ⬜ Planned |
| W-030 | CR-005 | PII erasure scheduled and job anonymises fields | Approved erasure request exists | Run `ErasePersonalDataJob` (or trigger via scheduler) | Lead PII fields (name, email, mobile, address) replaced with `[ERASED]`; `erased_at` timestamp set; audit log records erasure event; referential IDs and aggregate counters intact | ⬜ Planned |
| W-031 | CR-005 | PII erasure respects 30-day window | Erasure request created today | Check `scheduled_erasure_at` value | `scheduled_erasure_at` set to `requested_at + 30 days`; job does not run erasure before this date | ⬜ Planned |
| W-032 | CR-006 | Data residency check blocks non-India storage in production | `APP_ENV=production`; storage driver configured to `us-east-1` | Attempt to upload a file (e.g. lead attachment) | `DataResidencyCheck` middleware rejects the request with an HTTP 422 or redirect; error message indicates India-only storage enforcement | ⬜ Planned |
| W-033 | CR-006 | Data residency check is advisory in development | `APP_ENV=local`; storage driver configured to `us-east-1` | Attempt to upload a file | Upload proceeds; warning logged to application log but no request blocked | ⬜ Planned |
| W-034 | CR-007 | Audit trail does not expose raw PII | Audit log entries exist for lead edits containing mobile and email changes | Navigate to **Admin → Audit Trail**; view changed_fields for a lead update | Mobile and email values in `changed_fields_json` are masked or replaced; no raw PII values displayed in the UI or API response | ⬜ Planned |
| W-035 | CR-008 | DLT template validation logs warning for unregistered SMS | An SMS notification template not registered in DLT is used | Trigger an SMS dispatch using the unregistered template | `DltTemplateValidatorService` detects unregistered template; warning logged to application log; SMS send proceeds (advisory v1); no exception thrown | ⬜ Planned |
| W-036 | CR-009 | DPA page loads and PDF download works | `institution-admin` logged in | Navigate to **Compliance → Data Processing Agreement** | DPA page renders; **Download PDF** button visible; clicking it initiates download of the DPA PDF file from `storage/dpa/` | ⬜ Planned |
| W-037 | CR-009 | Non-admin cannot download DPA | User logged in with `counsellor` role | Navigate to `/crm/compliance/dpa` | HTTP 403 Forbidden | ⬜ Planned |
| W-038 | CR-010 | Security incident reported and breach notification dispatched | `institution-admin` logged in | Navigate to **Compliance → Security Incidents → Report**; fill in type, description, detection time; click **Submit** | `SecurityIncident` record created with status Reported; `BreachNotificationJob` dispatched; notification email sent to all institution admins; `notified_at` populated; SLA target is within 72 hours of `detected_at` | ⬜ Planned |
| W-039 | AL-001 | Application status transition to ENROLLED creates alumni pipeline record | Application in Admitted status exists | Update application status to `ENROLLED` (via status form or Artisan) | `GraduationObserver` fires; `AlumniPipelineService::enqueue()` called; `AlumniPipeline` record created with status Pending; `application_id` and `lead_id` populated | ⬜ Planned |
| W-040 | AL-001 | Alumni pipeline record progresses through statuses | Alumni pipeline record with status Pending exists | Admin reviews alumni record and marks Eligible | Status transitions from Pending → Eligible; subsequent sync action transitions to Synced | ⬜ Planned |
